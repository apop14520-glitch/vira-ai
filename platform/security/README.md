# Platform security

As primeiras políticas executáveis estão em `apps/api/app/security`: autenticação por ambiente, autorização administrativa, tokens somente via runtime e rate limit em buscas Foursquare. A gestão de secrets externa e o limiter distribuído continuam sendo responsabilidades da implantação futura.
